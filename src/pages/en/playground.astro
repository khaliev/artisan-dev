---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="Playground" description="Experiments, components, and prototypes.">
  <div class="container">
    <header class="page-header">
      <h1>Playground</h1>
      <p class="subtitle">Digital lab, experimental components, and prototypes.</p>
    </header>
  </div>
</BaseLayout>

<style>
  .page-header {
    padding-top: var(--space-8);
    padding-bottom: var(--space-8);
  }
  h1 {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-2);
  }
  .subtitle {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
  }
</style>
