---
import BaseLayout from '../layouts/BaseLayout.astro';
import ArticleCard from '../components/ArticleCard.astro';
import { getCollection } from 'astro:content';

const articles = await getCollection('blog', ({ data }) => data.locale === 'fr' && !data.draft);

const sortedArticles = articles.sort(
  (a, b) => new Date(b.data.datePublished).getTime() - new Date(a.data.datePublished).getTime()
);
---

<BaseLayout
  title="Blog / Journal"
  description="Articles, réflexions et notes techniques sur le développement web, l'éco-conception et l'accessibilité par Adlan."
>
  <div class="container">
    <header class="page-header">
      <h1 class="page-title">Blog / Journal</h1>
      <p class="subtitle">
        Réflexions, guides techniques et notes de journal sur l'éco-conception web, la performance et l'artisanat numérique.
      </p>
    </header>

    {sortedArticles.length > 0 ? (
      <div class="articles-list">
        {sortedArticles.map((article) => (
          <ArticleCard article={article} />
        ))}
      </div>
    ) : (
      <p class="empty-state">Aucun article publié pour le moment.</p>
    )}
  </div>
</BaseLayout>

<style>
  .page-header {
    padding-top: var(--space-8);
    padding-bottom: var(--space-8);
    max-width: 720px;
  }

  .page-title {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-2);
  }

  .subtitle {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    line-height: var(--leading-relaxed);
  }

  .articles-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
    max-width: 800px;
    margin-top: var(--space-4);
  }

  .empty-state {
    color: var(--color-text-muted);
    font-style: italic;
    padding-top: var(--space-8);
  }
</style>
