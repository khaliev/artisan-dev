---
import BaseLayout from '../layouts/BaseLayout.astro';
import ProjectCard from '../components/ProjectCard.astro';
import SectionHeading from '../components/SectionHeading.astro';
import TestimonialCard from '../components/TestimonialCard.astro';
import { getCollection } from 'astro:content';

const testimonials = [
  {
    name: 'Claire',
    role: 'Gérante de boutique',
    quote: 'Un accompagnement de A à Z : le site est rapide, élégant et a immédiatement augmenté nos demandes de rendez-vous.',
    photo: '/avatars/claire.jpg',
  },
  {
    name: 'Thomas',
    role: 'Restaurateur',
    quote: 'Livraison dans les délais, communication claire et un résultat à la hauteur. Je recommande vivement.',
    photo: '/avatars/thomas.jpg',
  },
  {
    name: 'Sophie',
    role: 'Architecte d’intérieur',
    quote: 'Adlan a su traduire notre univers en un site accessible et soigné. Le travail est propre, précis, professionnel.',
    photo: '/avatars/sophie.jpg',
  },
];

const projects = await getCollection('projects', ({ data }) => data.locale === 'fr');

const sortedProjects = projects.sort((a, b) => {
  if (a.data.featured && !b.data.featured) return -1;
  if (!a.data.featured && b.data.featured) return 1;
  return new Date(b.data.date).getTime() - new Date(a.data.date).getTime();
});
---

<BaseLayout
  title="Projets"
  description="Découvrez les réalisations et projets web sur-mesure développés par Adlan, artisan développeur web à Reims (Grand Est, France)."
>
  <div class="container">
    <header class="page-header">
      <h1 class="page-title">Projets</h1>
      <!-- Phrase définitionnelle GEO -->
      <p class="subtitle">
        Découvrez les réalisations et projets web sur-mesure conçus par Adlan, développeur web indépendant à Reims (Grand Est, France). 
        Chaque projet est optimisé pour la vitesse, la sobriété numérique et l'accessibilité.
      </p>
    </header>

    {sortedProjects.length > 0 ? (
      <div class="projects-grid">
        {sortedProjects.map((project) => (
          <ProjectCard project={project} />
        ))}
      </div>
    ) : (
      <p class="empty-state">Aucun projet disponible pour le moment.</p>
    )}

    <section class="testimonials-section" data-reveal>
      <SectionHeading
        eyebrow="Avis clients"
        title="Ils m'ont fait confiance"
        subtitle="Ce que disent celles et ceux qui m'ont confié leur projet."
      />
      <div class="testimonials-grid">
        {testimonials.map((t) => (
          <TestimonialCard name={t.name} role={t.role} quote={t.quote} photo={t.photo} />
        ))}
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .page-header {
    padding-top: var(--space-8);
    padding-bottom: var(--space-8);
    max-width: 800px;
  }

  .page-title {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-2);
  }

  .subtitle {
    font-size: var(--text-lg);
    color: var(--color-text-muted);
    line-height: var(--leading-relaxed);
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(1, minmax(0, 1fr));
    gap: var(--space-8);
    margin-top: var(--space-4);
  }

  @media (min-width: 768px) {
    .projects-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  .empty-state {
    color: var(--color-text-muted);
    font-style: italic;
    padding-top: var(--space-8);
  }

  .testimonials-section {
    padding-top: var(--space-24);
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(1, minmax(0, 1fr));
    gap: var(--space-6);
  }

  @media (min-width: 768px) {
    .testimonials-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }
</style>
